RewriteEngine on

# If the request is not for an existent root directory
RewriteCond %{REQUEST_FILENAME} !-d

# And the request is not for an existent root file
RewriteCond %{REQUEST_FILENAME} !-f

# Then, rewrite the request to "/subfolder"
RewriteRule ^([^/]+)/?$ /subfolder/$1 [NC,L]